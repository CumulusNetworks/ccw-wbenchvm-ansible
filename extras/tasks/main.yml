# list extra packages here
- name: install useful user packages
  apt: name={{item}}
  sudo: yes
  with_items:
    - vim-nox
    - tree
    - nano
    - telnet

- name: Download ccw wbenchvm tools
  git: repo=https://github.com/cumulusnetworks/ccw-wbenchvm-tools.git dest=/usr/src/

- name: symlink the cwng scripts
  file: src={{ item }} dest=/usr/local/bin/{{item | basename}} state=link
  with_fileglob:
    - /usr/src/ccw-wbenchvm-tools/cwng*
