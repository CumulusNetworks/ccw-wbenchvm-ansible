#! /bin/sh
# CUMULUS-AUTOPROVISIONING

echo -e "{{ switch_password }}\n{{ switch_password }}" | passwd cumulus

{{ ztp_data }}

adduser cwng
echo -e "cwng\ncwng" | passwd cwng

# CUMULUS-AUTOPROVISIONING
exit 0
